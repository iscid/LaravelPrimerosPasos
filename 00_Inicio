# Instalar Composer

# Verificar la versión
composer --version

# Instalar/actualizar Laravel
composer global require laravel/installer

# https://php.new/

# Cambiar la versión de PHP

set "PATH=C:\php-8.5.10;%PATH%"

# Comprobar la versión

php -v

PHP 8.5.10 (cli)

where php

C:\php-8.5.10\php.exe
C:\php\php.exe

# Comprobamos las extensiones

php -m | findstr /i "openssl curl mbstring pgsql fileinfo"

php artisan about



# Crear el proyecto
laravel new nombre_proyecto

# Solución al error Pail

Abrir composer.json y cambiar el arreglo "dev" que está dentro de Scripts por:

"dev": [
        "Composer\\Config::disableProcessTimeout",
        "npx concurrently -c \"#93c5fd,#c4b5fd,#fb7185\" \"php artisan serve\" \"php artisan queue:listen --tries=1 --timeout=0\" \"npm run dev\" --names=server,queue,vite --kill-others"
]
